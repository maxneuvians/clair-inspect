apiVersion: v1
kind: Service
metadata:
  labels:
    name: clair-db
  name: clair-db
  namespace: clair-inspect
spec:
  type: NodePort
  ports:
    - port: 6012
      targetPort: 6012
  selector:
    app: clair-db

